-r requirements.txt
# Pinned like the runtime deps: an unpinned dev stack makes CI results
# depend on the day they ran.
pytest==9.1.1
httpx==0.28.1
ruff==0.16.5
pip-audit==2.10.1
mypy==2.3.1
pytest-cov==7.1.0
